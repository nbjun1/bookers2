<h1>Users#show</h1>

      <%= render 'users/info', user: @user %>

      <%= render 'books/new', book: @newbook %>
      </div>

      <div class="col-md-8">
       <h1>Books</h1>
       <table>
        <thead>
         <tr>
          <th>Title</th>
          <th>Opinion</th>
         </tr>
        </thead>

        <tbody>
         <% @books.each do |book| %>
         <tr>
          <td><%= link_to book.title, book_path(book.id) %></td>
          <td><%= book.body %></td>
         </tr>
         <% end %>
        </tbody>
       </table>
      </div>

  </div>
</div>