
<h1>Books#show</h1>

<%= render 'users/info', user: @user %>

<%= render 'books/new', book: @book %>

<div class="col-md-8">
       <h1>Book detail</h1>
       <table>
        <thead>
         <tr>
          <th></th>
          <th></th>
          <th colspan="3"></th>
         </tr>
        </thead>

        <tbody>
         <% @books.each do |user| %>
         <tr>
          <td><%#= user.name %></td>
          <td><%= book.title %></td>
          <td><%= book.introduction %></td>
         </tr>
         <% end %>
        </tbody>
       </table>
      </div>

  </div>
</div>