
<h1>Books#show</h1>

<%= render 'users/info', user: @user %>

<%= render 'books/new', book: @book %>

<div class="col-md-8">
       <h1>Book detail</h1>
       <table>
        <thead>
         <tr>
          <th></th>
          <th colspan="2"></th>
          <th colspan="2"></th>
         </tr>
        </thead>

        <tbody>

         <tr>
          <td>
           <% if @user.profile_image.attached? %>
            <%= image_tag @user.profile_image, siza: "50x50" %>
           <% else %>
            <%= image_tag 'default-image', size: "50x50" %>
           <% end %>
           <p><%= link_to @user.name, user_path(@user.id) %></p>
          </td>
          <td><%= link_to @showbook.title, book_path %></td>
          <td><%= @showbook.body %></td>
          <td><%= link_to 'Edit', edit_book_path(@showbook.id) %></td>
          <td><%= link_to 'Destroy', @books, method: :delete, data: { confirm: "本当に消しますか？" } %></td>
         </tr>

        </tbody>
       </table>
      </div>

  </div>
</div>